{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "cat ${CLAUDE_PLUGIN_ROOT}/rules/*.md"
          },
          {
            "type": "command",
            "command": "cd ${CLAUDE_PLUGIN_ROOT} && uv run python ${CLAUDE_PLUGIN_ROOT}/hooks/load_active_context.py"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/stop_hook.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "mcp__cc-memory__add_decision",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/remind_task_on_decision.sh"
          }
        ]
      }
    ]
  }
}
